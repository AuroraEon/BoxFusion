# RSLG-SLAM Canonical Pipeline Runbook

RSLG-SLAM builds a rich-semantic, light-geometry indoor world model from RGB-D images and provided camera poses. Dataset-side SLAM or localization accuracy is not claimed because poses are provided.

## Canonical Layer Chain

`Layer 0: Input Layer -> Layer 1: World Model Layer -> Layer 2: Formal Artifact Layer -> Layer 3: Navigation Interface Layer -> Layer 4: Runtime Validation Layer`

Layer 1 is the World Model Layer. Stage-A is only the historical implementation entrypoint.

## Commands and Environments

Run from `/home/ws/workspace/BoxFusion`.

| Layer | Wrapper | Python | Canonical output |
| --- | --- | --- | --- |
| Layer 0: Input Layer | input checks are part of Layer 1 preflight | offline | scene input references |
| Layer 1: World Model Layer | `tools/rslg_pipeline/run_layer1_world_model.sh` | `/home/ws/miniconda3/envs/boxfusion/bin/python` | `stage_outputs/rslg_slam/00843-DYehNKdT76V/canonical/layer1_world_model/` |
| Layer 2: Formal Artifact Layer | `tools/rslg_pipeline/run_layer2_formal_artifacts.sh` | `/home/ws/miniconda3/envs/boxfusion/bin/python` | `.../canonical/layer2_formal_artifacts/` |
| Layer 3: Navigation Interface Layer | `tools/rslg_pipeline/run_layer3_navigation_interface.sh` | `/home/ws/miniconda3/envs/boxfusion/bin/python` | `.../canonical/layer3_navigation_interface/` |
| Layer 4: Runtime Validation Layer | `tools/rslg_pipeline/run_layer4_runtime_validation_static.sh` | offline packaging; `/usr/bin/python3` only for ROS runtime | `.../canonical/layer4_runtime_validation/` |

`tools/rslg_pipeline/run_rslg_pipeline_static.sh` validates existing canonical Layer 1-3 outputs and regenerates Layer 4 static packaging. Set `RSLG_PIPELINE_REBUILD=1` only when Layer 2 and Layer 3 regeneration is intended. Layer 1 execution is separately gated by `RSLG_LAYER1_ALLOW_RERUN=1`.

## Validation

- Layer 1: inspect `reports/canonical_layer1_world_model_validation_report_v0_1.json`.
- Layer 2: inspect its canonical manifest, integration reports, and object blocker.
- Layer 3: inspect `reports/layer3_navigation_interface_validation_report_v0_1.json` and the selected executable route candidate.
- Layer 4: inspect `reports/runtime_preflight_report_v0_1.json`, `reports/layer4_static_packaging_validation_report_v0_1.json`, and `reports/runtime_claim_boundary_report_v0_1.json`.

## Current Scene Status

For `00843-DYehNKdT76V`, Layer 1 is complete, Layer 2 is complete with the object approach blocked, and Layer 3 selected the `conservative_canonical` cross-floor room route. Layer 4 static packaging is generated by task38. Runtime execution still requires explicit authorization.

Do not claim dense reconstruction, dataset-side localization accuracy, neural implicit SLAM, a full embodied navigation benchmark, AMCL success, real robot execution, physical stair climbing, gait, footstep or contact planning, or a collision-free guarantee.
