# Deprecated Step30S5 Room15 Patch

These files were removed from the active `maps/` directory during Step30S7.

They are retained only as historical evidence and must not be used by current runtime tools, map profiles, RViz display, wall-crossing validation, or request-aware projection comparison.

Current active map profiles are `h8r2`, `step30s7_request_aware`, and `auto`.
